version: '3.5'
services:
  api:
    image: {tag}:latest
    env_file:
      - {local_folder}/.env
    networks:
      godlydnett:
        ipv4_address: {ip}
networks:
  godlydnett:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: {network}
